{
  "name": "seed-graph",
  "version": "0.1.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "oldauth": "graph auth https://api.thegraph.com/deploy/ $GRAPH_ACCESS_TOKEN",
    "auth": "graph auth --product hosted-service $GRAPH_ACCESS_TOKEN",
    "olddeploy": "graph deploy tenfinney/seed-graph --ipfs https://api.thegraph.com/ipfs/ --node https://api.thegraph.com/deploy/ --debug",
    "deploy": "graph deploy --product hosted-service tenfinney/polygon-seeds --debug",
    "format": "prettier --ignore-path .gitignore --write \"{*,**/*}.{ts,tsx,js,jsx,json,md,sol}\""
  },
  "license": "MIT",
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.30.4",
    "@graphprotocol/graph-ts": "^0.27.0",
    "keytar": "^7.9.0",
    "mustache": "^4.0.1"
  }
}
